# duplex-voice web server dependencies (ASCII only - Windows pip uses locale encoding)
fastapi>=0.111
uvicorn>=0.30
websockets>=12
httpx>=0.27
httpx-sse>=0.4
numpy>=1.26
# desktop demo extra deps (optional)
sounddevice>=0.5
silero-vad>=5.0          # PyTorch VAD (VADIterator)
onnxruntime>=1.18        # Silero ONNX backend
PyYAML>=6.0
